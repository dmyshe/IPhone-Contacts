import Foundation
import UIKit


struct Constants {
    struct UI {
        struct Layout {
            static let defaultPadding: CGFloat = 16
            static let defaultOffset : CGFloat = 8
        }
    }
    
}
